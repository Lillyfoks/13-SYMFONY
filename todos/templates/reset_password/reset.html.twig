{% extends 'base.html.twig' %}

{% block title %} {{ 'reset'|trans }}{% endblock %}

{% block body %}
    <h1> {{ 'reset'|trans }}</h1>

    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <button class="btn btn-primary"> {{ 'resetButton'|trans }}</button>
    {{ form_end(resetForm) }}
{% endblock %}
